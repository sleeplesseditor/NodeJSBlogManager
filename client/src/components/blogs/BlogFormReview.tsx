import * as React from 'react';

interface IProps {
    onEdit: () => void;
}

const BlogFormReview = (props: IProps) => {
    return (
        <form>
            
        </form>
    )
}

export default BlogFormReview;